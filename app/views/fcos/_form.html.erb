<%= form_with model:@fco do |f| %>

<%= f.label :name %>
<%= f.text_field :name %>

<br><br>

<%= f.label :pmu %>
<%= f.collection_select :pmu_id, Pmu.all, :id, :name %>

<br><br>

<%= f.submit %>

<% end %>