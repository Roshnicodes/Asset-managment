<%= form_with model:@pmu do |f| %>

<%= f.label :name %>
<%= f.text_field :name %>

<br><br>

<%= f.label :district %>
<%= f.collection_select :district_id, District.all, :id, :name %>

<br><br>

<%= f.submit %>

<% end %>