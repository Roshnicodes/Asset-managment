<%= form_with(model: product_variety) do |form| %>
  <div class="app-form-card">
    <% if product_variety.errors.any? %>
      <div class="app-form-errors">
        <h2><%= pluralize(product_variety.errors.count, "error") %> found</h2>
        <ul>
          <% product_variety.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="app-form-grid two-col">
      <div class="app-form-field">
        <%= form.label :stakeholder_category_id, "Stakeholder" %>
        <% if admin_user? %>
          <%= form.collection_select :stakeholder_category_id, @stakeholders, :id, :name, { prompt: "Select stakeholder" }, class: "form-control" %>
        <% else %>
          <div class="app-readonly-value"><%= current_employee_master&.stakeholder_category&.name || "-" %></div>
          <%= form.hidden_field :stakeholder_category_id, value: current_employee_master&.stakeholder_category_id %>
        <% end %>
      </div>

      <div class="app-form-field">
        <%= form.label :product_id, "Product" %>
        <%= form.grouped_collection_select :product_id, @themes, :products, :name, :id, :name, { prompt: "Select product" }, class: "form-control" %>
      </div>

      <div class="app-form-field">
        <%= form.label :product_type_code, "Product Type Code" %>
        <%= form.text_field :product_type_code, class: "form-control", placeholder: "Enter product type code" %>
      </div>

      <div class="app-form-field">
        <%= form.label :name, "Product Type" %>
        <%= form.text_field :name, class: "form-control" %>
      </div>
    </div>

    <div class="app-form-actions">
      <%= form.submit "Save Product Type", class: "app-form-submit" %>
    </div>
  </div>
<% end %>
