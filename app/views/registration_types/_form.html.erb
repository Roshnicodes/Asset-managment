<%= form_with(model: registration_type) do |form| %>
  <% if registration_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(registration_type.errors.count, "error") %> prohibited this registration_type from being saved:</h2>

      <ul>
        <% registration_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
