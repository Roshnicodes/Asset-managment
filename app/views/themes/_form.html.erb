<%= form_with model: @theme do |f| %>
  <div class="mb-3">
    <%= f.label :name, "Thematic Type Name", class: "form-label" %>
    <%= f.text_field :name, class: "form-control" %>
  </div>

  <%= f.submit class: "btn btn-primary" %>
<% end %>
