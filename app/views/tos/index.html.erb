<div class="app-toolbar">
  <h1>TO</h1>
  <%= link_to "Add TO", new_to_path, class: "app-primary-btn" %>
</div>

<div class="app-table-wrap">
  <table class="table app-table">
    <thead>
      <tr>
        <th>TO Name</th>
        <th>FCO</th>
        <th>Action</th>
      </tr>
    </thead>
    <tbody>
      <% @tos.each do |to| %>
        <tr>
          <td><%= to.name %></td>
          <td><%= to.fco.name %></td>
          <td class="app-row-actions">
            <%= link_to "Edit", edit_to_path(to) %> |
            <%= link_to "Delete", to, method: :delete, data: { turbo_confirm: "Delete this TO?" } %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
