<div class="app-page-header">
  <h2>Vendor Registration Form</h2>
  <%= link_to "Back", vendor_registrations_path, class: "app-back-link" unless @public_registration_form %>
</div>

<%= render "form", vendor_registration: @vendor_registration, form_url: @vendor_registration_form_url %>
